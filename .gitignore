# Editores / IDE
.idea
.vscode

# Sistema operativo
Thumbs.db
.DS_Store

# Node (por si más adelante se usa alguna herramienta)
node_modules
dist

# Archivos comprimidos / descargas (no versionar)
*.zip

# La carpeta img solo debe contener imágenes, no páginas HTML guardadas
img/*.html
